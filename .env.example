# Bluesky Credentials
BLUESKY_HANDLE=labeler.boio.la
BLUESKY_PASSWORD=xxxx-xxxx-xxxx-xxxx

# MySQL Database (Hostgator)
DB_HOST=localhost
DB_USER=seu_usuario
DB_PASSWORD=sua_senha
DB_NAME=nome_do_banco

# Flask
PORT=5000
```

### 🎬 COMO:

1. GitHub → **"Add file"** → **"Create new file"**
2. Nome: `.env.example`
3. Colar conteúdo
4. Commit: "Add env example"

---

## ⚙️ PASSO 4: Configurar Variáveis no Render

### 🌍 ONDE: **Render Dashboard**

### 🎬 COMO:

1. Ir em: https://dashboard.render.com
2. Clicar em: **"diva-labeler"**
3. Menu lateral → **"Environment"**
4. Clicar: **"Add Environment Variable"**

### 📝 ADICIONAR (uma por uma):

**Você JÁ TEM estas (não mexa):**
```
BLUESKY_HANDLE = labeler.boio.la
BLUESKY_PASSWORD = (já configurado)
PORT = 10000
```

**ADICIONAR NOVAS:**
```
DB_HOST = 
DB_USER = 
DB_PASSWORD = 
DB_NAME =
