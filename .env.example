# Bluesky Credentials
BLUESKY_HANDLE=labeler.boio.la
BLUESKY_PASSWORD=xxxx-xxxx-xxxx-xxxx

# MySQL Database (Hostgator)
DB_HOST=localhost
DB_USER=seu_usuario
DB_PASSWORD=sua_senha
DB_NAME=nome_do_banco

# Flask
PORT=5000
```

**Com valores FALSOS/EXEMPLO!** Isso é só pra documentar quais variáveis existem.

---

## 🔐 ONDE COLOCAR AS CREDENCIAIS REAIS?

### **NO RENDER! (Environment Variables)**

As credenciais **NUNCA** vão pro GitHub. Elas ficam **APENAS** no Render!

---

## ⚙️ PASSO A PASSO: Configurar no Render

### 1. Ir no Render Dashboard
https://dashboard.render.com

### 2. Clicar em "diva-labeler"

### 3. Menu lateral → "Environment"

### 4. Adicionar variáveis (uma por uma):

#### **DB_HOST:**
```
DB_HOST = localhost
```
✅ **Sim, use `localhost`!** 

Porque o MySQL do Hostgator está no mesmo servidor que o PHP.

⚠️ **MAS ATENÇÃO:** O Render está **FORA** do Hostgator, então provavelmente **NÃO vai funcionar** com `localhost`.

**Possíveis valores para DB_HOST:**

**Opção A:** IP do servidor Hostgator
```
DB_HOST = 192.168.1.100  (exemplo)
```

**Opção B:** Hostname do MySQL
```
DB_HOST = mysql.boio.la  (se tiver)
